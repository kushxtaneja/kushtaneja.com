# kushtaneja.com

Kush Taneja's personal website: writing, experiments, and a generative nature
sound mixer.

The site is plain HTML, CSS, and JavaScript. Open `index.html` directly to run
it; there is no build step or local server.

## Migration status

The new homepage is self-contained, but writing links still point to the
existing Posthaven pages. Migrate those posts into this repository before
moving the domain away from Posthaven.
